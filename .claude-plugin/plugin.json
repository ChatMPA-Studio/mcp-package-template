{
  "name": "{{package_name}}",
  "version": "{{version}}",
  "description": "{{description}} — connects to the MCP server and keeps its skills synced locally every session.",
  "author": {
    "name": "{{AUTHOR_NAME}}",
    "email": "{{AUTHOR_EMAIL}}"
  },
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}\"/scripts/sync_skills.sh"
          }
        ]
      }
    ]
  }
}
